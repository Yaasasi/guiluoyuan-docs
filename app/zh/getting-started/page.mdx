# 快速开始

## 本地开发

```bash
npm run dev
```

访问 `http://localhost:3000`，将自动跳转到 `http://localhost:3000/zh`。

## 生产构建

```bash
npm run build
npm run start
```

`build` 后会自动执行 `postbuild`，在 `public/_pagefind` 生成全文搜索索引。
